batch_size: 128
embedding_dim: 300
hidden_sizes: '300_300_10000'
n: 5
debug: False
num_epochs: 5
dropout_p: 0.2
l1_scalar: 0.1
disable_cuda: False
dataset_path: 'data/msmarco/'
embedding: 'glove'
glove_embedding_path: 'embeddings/glove.6B.300d.txt'
bert_embedding_path: 'embeddings/bert'
hydra:
  run:
    dir: experiments/l1_scalar_${l1_scalar}_h_sizes_${hidden_sizes}_n_${n}_drop_${dropout_p}_emb_${embedding}_batch_size_${batch_size}
